# 2026-09-10

- 运行 setup-matt-pocock-skills 初始化工程 skills 配置：issue 跟踪选本地 Markdown（`.scratch/<feature-slug>/`，无 git remote），triage 标签保留默认五角色，域文档 single-context。
- 新增 `AGENTS.md`（Agent skills 配置块）及 `docs/agents/issue-tracker.md`、`docs/agents/triage-labels.md`、`docs/agents/domain.md`。
- `git init -b main` 初始化仓库，提交 ed08e87（128 文件，Flutter 脚手架 + agent skills 配置）。无 remote，本地 Markdown issue 跟踪仍适用。
- 移除桌面平台目录（linux/macos/windows，提交 d084678），项目定位 Android 优先 + iOS 尽量兼容。
- 工单 02 完成（147b1b4 + 修正 d4f6b0c）：core/model + core/rules 徐医规则包，28 测试全绿，core/ 零 Flutter import。环境坑：本机 flutter test 需要 `env PROGRAMFILES(X86)= http_proxy= https_proxy=` 前缀（%PROGRAMFILES(X86)% 缺失 + 代理拦截 flutter_tester WebSocket）。
- 工单 01/03/05 完成（27a872b/1677fb7/2e20182）：App 壳（liquid_glass_widgets 1.4.2 + ui/glass 封装层）、jqGrid 解析器（sealed GradeParseResult，incompleteColumns/noTable 失败语义）、drift 仓储（固定 uuid 默认档案幂等）。全仓 59 测试全绿。坑：drift 须锁 2.30.1（2.35 与 Flutter meta 1.17 冲突）。
- 并行启动 06 成绩页 UI 与 07 WebView 采集（features/gpa 目录写冲突靠文件边界划分：06 不碰 capture_entry_button.dart，07 只改该文件 + capture_page.dart）。
- subagent 多次 429 限流（14:33 重置）后主 agent 接手：06/07/08/09/10 全部由主 agent 完成。坑：①GlassScaffold 是 Cupertino 系，Material 组件（TextField/Dropdown/Slider）必须包透明 Material 祖先否则红屏（main_shell + capture_page 已统一兜底）；②file_picker 12.x API 变了（FilePicker.pickFiles 静态 + readAsBytes，platform getter 没了）；③riverpod 3 没有 valueOrNull 用 .value；④ListView 懒加载下测试断言视口外分区要 skipOffstage:false。
- 用户明确：**以后不要管打包**（flutter build apk 环境冲突 JDK 25/Kotlin，用户自己处理）。已记入 ~/.workbuddy/MEMORY.md。
- GPA v1 十工单全部 done。剩 code-review 全量审查未跑。
- 真机验证修复：①教务登录页是明文 HTTP（Android 9+ 禁 cleartext → ERR_CLEARTEXT_NOT_PERMITTED），network_security_config 仅对 jwpt/ehall.xzhmu.edu.cn 放行（ba79960）；②采集入口 URL 必须是 ehall.xzhmu.edu.cn（统一认证入口，直连 jwpt 无法登录）（076e4aa）。
- **iOS 26 Liquid Glass UI 全面重写**（未提交，工作区）：用户明确纠正方向——**iOS 26 的设计语言核心就是 Liquid Glass，不能只堆 Cupertino 扁平组件**。定案"全面玻璃 + 保留 kit 令牌层"。
  - 新增 `lib/ui/kit/`（设计系统唯一入口）：`tokens.dart`（AppTokens 语义色 + AppText 排版，取代 Material ColorScheme.fromSeed 的靛紫，根治黑紫混杂）、`glass_theme_bridge.dart`（AppTokens → GlassThemeData/GlassGlowColors，注入玻璃主题）、`surfaces.dart`（AppGlassScaffold/AppGlassAppBar/AppGlassTabBar/AppGlassCard/AppWallpaper）、`grouped.dart`（AppGlassGroupedCard/AppGlassGroupedSection/AppGlassListTile/AppStatRow/AppCardTitle/AppDivider）、`controls.dart`（AppGlassButton/AppGlassSegmentedControl/AppGlassSlider/AppGlassSwitch/AppGlassProgress/AppGlassTextField/AppGlassPicker + showAppAlert/showAppPicker）。
  - `lib/ui/glass/`（旧玻璃封装层）已被 kit 完全取代 → 删除，避免双封装。
  - 重写：app/theme.dart（Material ColorScheme 由 AppTokens 显式构造）、main.dart（LiquidGlassWidgets.initialize + wrap(theme: 桥接)/brightnessResolver）、app.dart（MaterialApp 保留以承载明暗解析）、main_shell.dart（AppGlassScaffold + AppTabBar + selectedIcon）、7 个 GPA/Settings 特性页。
  - **关键坑**：GlassScaffold 基于 CupertinoPageScaffold，**无 Material 祖先** → 所有 Material-only 组件（TextField/showDialog/AlertDialog/TextButton/showMenu/PopupMenuItem）会抛 "No Material widget found"。已全部替换为 Cupertino/玻璃版：TextField→AppGlassTextField(GlassTextField)、AlertDialog→showAppAlert(GlassDialog.show)、showMenu→showAppPicker(CupertinoPicker)+AppGlassPicker(GlassPicker)。
  - 附带修复：GradeParseFailureReason 加 `.label` 中文扩展（原来 UI 直接插值枚举名）。
  - 环境：仍遵守"不管编译"——纯静态审计（import 解析、kit 符号对账、玻璃库 API 签名逐一核对 lib 源码、括号配平）。

- **提交 82a5557**（UI 重写）+ **bf69cda**（三个 UI 问题修复）已推 origin/main。bf69cda 内容：①AppGlassScaffold.extendBody 默认 false 解决顶部/底部内容顶边；②AppText 全量显式声明 decoration:none + AppText.textTheme 铺进 ThemeData，消除字体回退链上的黄色下划线；③MainShell 根级 DefaultTextStyle 兜底。

- **用户第二次纠正（关键）**：①标题应**嵌在页面内容里**（App Store 式 Large Title），不是独立 app bar；②底栏不该"只有一半透明"——应保持 `extendBody: true` + **页面自己加额外 padding 让内容滚过去**。bf69cda 用"不重叠"回避问题是错的方向。
  - 提交 **00f51dd**：骨架恢复 `extendBody: true`；新增 `AppGlassLargeTitle`（内嵌标题 sliver）；新增 `AppGlassMetrics` / `AppPagePadding`（页面自算上下留白）；主壳每 tab 持有独立 `GlassLargeTitleController`；分类页改 `CustomScrollView` + slivers。
  - **库的两个坑（源码级确认）**：①`GlassScaffold` 文档声称"自动处理 top/bottom padding"，但**源码无任何实现**——body 就是 `Positioned.fill` 原样铺满，页面必须自己加 spacer；②`GlassTabBar.bottom` 的 `preferredSize.height = barHeight(64) + verticalPadding(20)*2 = 104`，不是 64——误用 64 会让内容底部被裁 40pt。
  - `GlassLargeTitle` 是 **sliver**（返回 SliverToBoxAdapter），必须放 `CustomScrollView`；`GlassAppBar.largeTitleController` 与它联动实现折叠；`GlassLargeTitleController` 是 ChangeNotifier 且 `dispose()` 会 dispose 内部 scrollController（与 CustomScrollView 共用同一个），故只能 dispose 一次（主壳/测试各一处）。
  - 本轮同时**全量清 Material**：Icons.* → CupertinoIcons（school→book_solid、arrow_back→back、arrow_forward→chevron_forward、upload_file→doc_plaintext、cloud_download_outlined→cloud_download、check_circle→check_mark_circled_solid、error→exclamationmark_circle、rule_folder→checkmark_seal、apps→app、info→info_circle）；MaterialPageRoute → CupertinoPageRoute；修复 `AppGlassListTile.subtitle` 的 `Text(subtitle!)` **null 断言崩溃**（subtitle 可选却被强解包）。
  - 注意：`CupertinoIcons` 没有 `school` / `unfold_more` / `chevron_up_down`；正确的上下双箭头是 `chevron_up_chevron_down`。项目中 lib/app/{app,theme}.dart、main.dart、ui/kit/tokens.dart 仍 import material（MaterialApp 根 + ThemeData 承载玻璃明暗解析，属有意保留）。

